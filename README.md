# arkbunyan.github.io

My personal site. React + Vite, deployed to GitHub Pages.

```
npm install
npm run dev
npm run build
```

Text lives in `src/content.ts`. Styles in `src/styles/global.css`.

Pushing to main triggers the deploy workflow. Pages source has to be set to
GitHub Actions in the repo settings for that to work.
